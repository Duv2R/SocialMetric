console.log()
function abrirVentanaEmergente() {
    // URL o dirección del contenido que deseas mostrar en la ventana emergente
    var url = `http://${window.location.host}/Social-Metric/ventana-login-google.html`;

    // Propiedades de la ventana emergente
    var opcionesVentana = 'width=900,height=800';

    // Abrir la ventana emergente
    window.open(url, '_blank', opcionesVentana);
}
function abrirVentanaEmergente2() {
    // URL o dirección del contenido que deseas mostrar en la ventana emergente
    var url = `http://${window.location.host}/Social-Metric/ventana-login-facebook.html`;

    // Propiedades de la ventana emergente
    var opcionesVentana = 'width=900,height=800';

    // Abrir la ventana emergente
    window.open(url, '_blank', opcionesVentana);
}
function abrirVentanaEmergente3() {
    // URL o dirección del contenido que deseas mostrar en la ventana emergente
    var url = `http://${window.location.host}/Social-Metric/ventana-login-Instagram.html`;

    // Propiedades de la ventana emergente
    var opcionesVentana = 'width=900,height=800';

    // Abrir la ventana emergente
    window.open(url, '_blank', opcionesVentana);
}